# Gitea Environment Configuration

# Database password
DB_PASSWORD=gitea-secure-password

# Server configuration
ROOT_URL=https://git.apps.local
SSH_DOMAIN=git.apps.local

# Disable public registration (set to true for private instance)
DISABLE_REGISTRATION=false

# Runner token (get from Gitea Admin > Actions > Runners)
# Generate with: docker exec gitea gitea actions generate-runner-token
RUNNER_TOKEN=

# Runner name (optional)
RUNNER_NAME=docker-runner

# Runner labels (format: label:docker://image)
# Common labels for GitHub Actions compatibility
RUNNER_LABELS=ubuntu-latest:docker://node:20-bookworm,ubuntu-22.04:docker://node:20-bookworm,ubuntu-20.04:docker://node:18-bullseye

# Timezone
TZ=Europe/Paris
