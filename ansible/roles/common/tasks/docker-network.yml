---
# =============================================================================
# Common - Docker network setup
# =============================================================================

- name: Ensure Traefik public Docker network exists
  community.docker.docker_network:
    name: "{{ traefik_public_network | default('traefik-public') }}"
    state: present
  tags: [docker, network]

