# Gitea Secrets Reference

- **Admin Login**: `gitea_admin`
- **Admin Password File**: `.secrets/gitea-admin-password`
- **Database Password File**: `.secrets/gitea-db-password`
- **Secret Key File**: `.secrets/gitea-secret-key`

> Note: All secret files in `.secrets/` are strictly ignored by Git (`.gitignore`).
