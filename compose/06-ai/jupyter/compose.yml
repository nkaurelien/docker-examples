# JupyterLab Interactive Data Science & AI Notebook Stack
# Generated by Ansible / IaC

x-arcane:
  icon: https://raw.githubusercontent.com/selfhst/icons/main/svg/jupyter.svg
  urls:
    - https://jupyter.org/
    - https://github.com/jupyter/docker-stacks

services:
  jupyter:
    image: quay.io/jupyter/datascience-notebook:latest
    container_name: jupyter
    restart: unless-stopped
    command: start-notebook.sh --IdentityProvider.token='${JUPYTER_TOKEN:-secret}'
    environment:
      - DOCKER_STACKS_JUPYTER_CMD=lab
      - JUPYTER_ENABLE_LAB=yes
      - JUPYTER_TOKEN=${JUPYTER_TOKEN:-secret}
    volumes:
      - ./data:/home/jovyan/work
    networks:
      - ${TRAEFIK_PUBLIC_NETWORK:-traefik-public}
    labels:
      - "com.getarcaneapp.arcane.icon=https://raw.githubusercontent.com/selfhst/icons/main/svg/jupyter.svg"
      - "traefik.enable=true"
      - "traefik.http.routers.jupyter.rule=Host(`jupyter.${DOMAIN:-kamitbrains.fr}`) || Host(`notebook.${DOMAIN:-kamitbrains.fr}`)"
      - "traefik.http.routers.jupyter.entrypoints=websecure"
      - "traefik.http.routers.jupyter.tls.certresolver=letsencrypt"
      - "traefik.http.routers.jupyter.middlewares=crowdsec-bouncer@file"
      - "traefik.http.services.jupyter.loadbalancer.server.port=8888"
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://127.0.0.1:8888/lab || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 20s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  traefik-public:
    external: true
