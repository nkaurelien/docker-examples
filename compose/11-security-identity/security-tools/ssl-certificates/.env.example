# SSL Certificate Generators - Environment Configuration

# === omgwtfssl ===
# Certificate subject (main domain)
SSL_SUBJECT=localhost
# Alternative DNS names (comma-separated)
SSL_DNS=localhost,*.localhost,myapp.local,*.myapp.local
# Alternative IP addresses (comma-separated)
SSL_IP=127.0.0.1
# Key size in bits (2048 or 4096)
SSL_SIZE=2048
# Certificate validity in days
SSL_EXPIRE=365
# Certificate Authority name
CA_SUBJECT=my-ca
# CA validity in days
CA_EXPIRE=3650

# === mkcert ===
# Domains to generate certificates for (space-separated)
MKCERT_DOMAINS=localhost 127.0.0.1 ::1 myapp.local *.myapp.local
