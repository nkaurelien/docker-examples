# Pingvin Share

Pingvin Share is a self-hosted file sharing platform that allows you to share files via a link with password protection and expiration dates.

## Usage

```bash
docker compose up -d
```
