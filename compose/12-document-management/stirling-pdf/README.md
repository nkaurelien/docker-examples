# Stirling-PDF

Stirling-PDF is a powerful, locally hosted web application that allows you to perform various operations on PDF files (merge, split, OCR, convert, watermark, sign, etc.).

## Usage

```bash
docker compose up -d
```
