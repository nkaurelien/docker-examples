# Paperless-ngx environment configuration
# See https://docs.paperless-ngx.com/configuration/

# Set your user and group IDs to match the host system
# Run 'id -u' and 'id -g' to get these values
USERMAP_UID=1000
USERMAP_GID=1000

# OCR settings
PAPERLESS_OCR_LANGUAGE=eng
# Add more languages separated by '+', e.g.: eng+fra+deu

# Secret key for Django (generate a random string for production)
# PAPERLESS_SECRET_KEY=change-me-to-a-random-string

# Admin account (optional - can also be created via web UI)
# PAPERLESS_ADMIN_USER=admin
# PAPERLESS_ADMIN_PASSWORD=admin

# Time zone
PAPERLESS_TIME_ZONE=Europe/Paris

# Enable polling for network file systems (NFS)
# PAPERLESS_CONSUMER_POLLING=true
