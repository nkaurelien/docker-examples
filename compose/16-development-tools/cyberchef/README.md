# CyberChef

CyberChef is a simple, intuitive web app for carrying out all manner of "cyber" operations within a web browser (encoding, decoding, hashing, regex, data parsing).

## Usage

```bash
docker compose up -d
```
