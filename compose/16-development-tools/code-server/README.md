# Code-Server (VS Code Web)

Code-Server runs Visual Studio Code on any machine and lets you access it directly through the browser.

## Features
- Code from any device (tablet, laptop, browser).
- Integrated terminal and extension marketplace support.
- Built-in authentication.

## Usage

```bash
docker compose up -d
```
