# CouchDB Cluster Configuration Example
# Copy this file to .env and update with your actual values

# Docker Compose Project Configuration
COMPOSE_PROJECT_NAME=your-project-name
COMPOSE_PROJECT_DESC="Your CouchDB Cluster Description"

# CouchDB Cluster Configuration
COUCHDB_NODE_NAME=your-cluster-name
PORT_BASE=5984

# CouchDB Authentication (CHANGE THESE!)
COUCHDB_USER=admin
COUCHDB_PASSWORD=your-secure-password

# CouchDB Security Secrets (GENERATE NEW VALUES!)
# Generate with: openssl rand -base64 64
COUCHDB_SECRET=your-couchdb-secret-base64-encoded
COUCHDB_COOKIE=your-erlang-cookie-base64-encoded